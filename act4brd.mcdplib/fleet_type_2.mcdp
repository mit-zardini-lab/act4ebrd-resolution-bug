mcdp {
  provides number_t2    [car]

  requires capital_cost [USD]
  requires operators    [N]

  price = 850000 USD/car
  person = 1 N/car
  
  required capital_cost >= price  * provided number_t2
  required operators >= person * provided number_t2
}
