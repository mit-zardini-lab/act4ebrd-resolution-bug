mcdp {
  provides number_t2    [Nat]

  requires capital_cost [USD]
  requires operators    [Nat]

  price = 850000 [USD/car]
  required capital_cost >= price  * number_t2
  required operators >= number_t2
}