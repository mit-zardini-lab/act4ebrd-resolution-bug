mcdp {
  provides number_t1    [car]

  requires capital_cost [USD]
  requires operators    [Nat]

  price = 1200000 USD/car
  person = 1 dimensionless/car
  
  required capital_cost >= price * provided number_t1
  required operators >= person * provided number_t1
}
