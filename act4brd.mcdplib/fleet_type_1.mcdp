mcdp {
  provides number_t1    [Nat]

  requires capital_cost [USD]
  requires operators    [Nat]

  price = 1200000 [USD/car]
  required capital_cost >= price * number_t1
  required operators >= number_t1
}